name: CI (macOS)

on:
  push:
    branches:
      - main
      - worktree-add_macos_test
  pull_request:
    branches: [ main ]

jobs:
  test-macos:
    # Intel (x86_64) runner. The devcontainer Dockerfile pins the mise apt
    # source to arch=amd64, so an arm64 runner (macos-14/15 default) would
    # fail to install mise; arm64 runners also cannot run Colima's VM
    # (nested virtualization is unsupported -> HV_UNSUPPORTED). macos-13 is
    # retired, so use the current Intel image macos-15-intel.
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install Docker via Colima
        run: |
          # macOS GitHub runners do not ship a Docker daemon. Colima provides
          # a Linux VM with a Docker daemon that the devcontainer CLI can use.
          df -h /
          brew install colima docker
          # Default memory (2 GiB) causes OOM during the heavy image build, so
          # raise it. Colima makes itself the default docker context on start.
          colima start --cpu 4 --memory 12 --disk 30
          # Some tooling hardcodes /var/run/docker.sock instead of honoring the
          # docker context; symlink Colima's socket there as insurance.
          sudo ln -sf "$HOME/.colima/default/docker.sock" /var/run/docker.sock
          docker version
          docker info

      - name: Install devcontainer CLI and AI tools
        run: |
          npm install -g @devcontainers/cli
          # Antigravity CLI is a native binary installed to ~/.local/bin/agy (not on npm)
          curl -fsSL https://antigravity.google/cli/install.sh | bash
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Prepare mock files for mounts and authentication
        run: |
          mkdir -p ~/.claude ~/.gemini
          # Create dummy credential files to simulate web authentication
          echo '{"loggedIn": true, "dummy": true}' > ~/.claude/.credentials.json
          echo '{}' > ~/.claude.json
          git config --global user.email "you@example.com"
          git config --global user.name "Your Name"

      - name: Prepare environment
        run: |
          # Ensure GH_TOKEN is set for initializeCommand
          echo "GH_TOKEN=${{ secrets.GITHUB_TOKEN }}" > .devcontainer/.env
          # Mock SSH agent
          ssh-agent -a /tmp/ssh-agent.sock > /dev/null
          echo "SSH_AUTH_SOCK=/tmp/ssh-agent.sock" >> $GITHUB_ENV

      - name: Run test.sh
        run: .devcontainer/test.sh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Set dummy keys to see if the tools at least try to connect
          ANTHROPIC_API_KEY: "dummy"

      # Temporary diagnostics: inspect the firewall state that test.sh
      # exercised, talking to Docker directly (the devcontainer CLI loses
      # track of the container across steps).
      - name: Diagnose firewall (on failure)
        if: failure()
        run: |
          echo "=== docker ps -a ==="
          docker ps -a
          CID=$(docker ps -q | head -1)
          echo "running container id: '$CID'"
          if [ -z "$CID" ]; then
            echo "no running container; inspecting last container logs"
            LAST=$(docker ps -aq | head -1)
            docker logs "$LAST" 2>&1 | tail -30 || true
            exit 0
          fi
          docker exec "$CID" bash -euxc '
            echo "=== resolve github.com ==="
            getent ahosts github.com || true
            echo "=== iptables rules mentioning 140.82 / github ==="
            sudo iptables-save | grep -E "140\.82|github" || echo "(no 140.82 rules found)"
            echo "=== count of ACCEPT OUTPUT rules ==="
            sudo iptables-save | grep -c "A OUTPUT" || true
            echo "=== jq present? ==="
            which jq && jq --version || echo "jq MISSING"
            echo "=== re-fetch api.github.com/meta web ranges ==="
            curl -s -m 15 -H "Accept: application/vnd.github+json" https://api.github.com/meta | jq -r ".web[]" 2>&1 | head -25 || echo "meta fetch FAILED"
          ' || true
